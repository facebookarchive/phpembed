
Checkout phpembed/trunk
+++++++++++++++++++++++

(1) Update configure.ac. Change AM_INIT_AUTOMAKE version number. Commit.

(2) ./bootstrap.sh;  ./configure;

(3) Run "make dist"
    // creates a "phpembed-x.y.z.tar.gz" file

(4) Extract contents of tar gzip file. Remove all files / dirs except
    bootstrap.sh  configure.ac  COPYING  docs  examples  LICENSE  Makefile.am  NEWS phpembedVersion.h.in  README  SConstruct  src

   /rm aclocal.m4  AUTHORS ChangeLog  config.guess config.h.in config.sub configure depcomp install-sh ltmain.sh missing INSTALL Makefile.in 

(5) Re tar / gzip into a new file named "phpembed-<year><mon><day>.tar.gz

(6) Verify content of tar file. Try a build etc.

(7) Check into svn+ssh://tubbs/svnroot/projects/phpembed

(8) Hand over to Boz / Shire to opensource.
